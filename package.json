{
  "name": "y",
  "version": "1.0.0",
  "description": "A cli to fetch news based on dates and tags",
  "main": "bin/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "bin": {
    "newsfind": "./bin/index.js"
  }
}
